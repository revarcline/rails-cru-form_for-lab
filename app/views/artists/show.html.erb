<h1><%= @artist.name %></h1>
<p><%= @artist.bio %></p>
<h3>Songs by <%= @artist.name %>:</h3>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.name, song_path(song) %></li>
  <% end %>
</ul>
