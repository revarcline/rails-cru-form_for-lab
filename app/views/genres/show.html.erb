<h1><%= @genre.name %></h1>
<h3><%= @genre.name %> songs:</h3>
<ul>
  <% @genre.songs.each do |song| %>
    <li><%= link_to "#{song.name}, #{song.artist.name}", song_path(song) %></li>
  <% end %>
</ul>
